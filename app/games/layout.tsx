const Authlayout = ({ children }: { children: React.ReactNode }) => {
  return <main className="py-5 bg-gradient-to-b from-gray-50 to-gray-100 dark:from-gray-900 dark:to-gray-800">{children}</main>;
};

export default Authlayout;
